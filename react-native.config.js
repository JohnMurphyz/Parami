module.exports = {
  dependencies: {
    '@sentry/react-native': {
      platforms: {
        ios: null, // Disable auto-linking for iOS
        android: null, // Disable auto-linking for Android
      },
    },
  },
};
